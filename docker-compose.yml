version: "3.9"

services:
  dataflow:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      # Persist the SQLite config database between container restarts
      - ./dataflow_config.db:/app/dataflow_config.db
    environment:
      - ENV=production
    restart: unless-stopped