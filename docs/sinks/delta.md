# Delta Lake Sinks

Use the Delta sink to write a Delta Lake table. Dataflow writes with overwrite
mode and can partition output by one configured column.

```text
s3://analytics/delta/sales_daily
```
