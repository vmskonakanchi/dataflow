# SQL Transformations

Dataflow compiles built-in transformation steps into DuckDB SQL. Use select,
filter, aggregate, and join steps to shape data without writing a standalone
pipeline script.

Use the SQL Query Tool to explore a query before adding the equivalent
transformation steps to a pipeline.
