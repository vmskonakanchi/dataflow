# Running a Pipeline

Run a pipeline manually from the **Pipelines** page with **Run Now**. Dataflow
places a durable job on its worker queue and runs it in a separate worker
process.

To run automatically, create a schedule from the **Schedules** page. See
[Scheduling](../concepts/scheduling.md).
