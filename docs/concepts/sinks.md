# Sinks

Sinks define where Dataflow writes pipeline results. Current sink formats are
Parquet and Delta Lake. A sink path may use
[template variables](template-variables.md).

See the [sink reference](../sinks/) for format-specific behavior.
