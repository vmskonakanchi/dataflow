# Sources

Sources describe the input data for a pipeline. Dataflow currently reads local
paths, S3-compatible paths, HTTPS URLs, and glob patterns through DuckDB.

Source strings can use [template variables](template-variables.md). For
format-specific guidance, see the [source reference](../sources/).
